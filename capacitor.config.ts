import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'de.grob.capacitator.pwa',
  appName: 'WebApp',
  webDir: 'out',
  bundledWebRuntime: false,
}

export default config
